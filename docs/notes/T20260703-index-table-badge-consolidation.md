# Task T20260703: 会社一覧テーブルのバッジ統合・表示項目拡張

- 着手日: 2026-07-03
- 完了日: 2026-07-03
- 担当: iori-oiso（計画・Gate 承認）+ Claude Code（実装・検証）
- 関連リンク: なし

---

## 1. 把握・整理（ステップ 1）

### 解決すべき課題（1 行）

**会社一覧画面（`/v3/index`）のテーブルで、常時表示列（割安比率・グレアム指数）とアコーディオン展開時のバッジ（割安度・グレアム指数・最新企業価値）が完全に同一値で重複しており、開いても新情報が「最新企業価値」しかない状態を解消しつつ、未表示の指標（提出日株価平均・みんかぶ予想株価）を追加する。**

### スコープ確定

| 区分 | 内容 |
|---|---|
| **コア（必ず実装）** | `fragments/index-table.html` のデスクトップテーブル・モバイルカードを変更。①常時表示側の「割安比率」「グレアム指数」をバッジ風スタイル（色付き・サムズアップ/ダウンアイコン）に変更 ②常時表示バッジに「最新企業価値」「提出日株価平均」「みんかぶ予想株価」を追加（バッジ5種構成）③アコーディオン展開時のバッジ3枚（最新企業価値・割安度・グレアム指数）と「銘柄詳細を見る」リンクを削除し、展開時は summaryChart のみ表示 |
| **後回し** | なし |
| **対象外** | `IndexPresenter.java` の変更（`CorporateViewModel` に必要フィールド `averageStockPrice` / `forecastStock` が既に存在するため不要）、`ALLOWED_SORT_FIELDS` の拡張（ユーザー確認済みで対象外）、PER/PBR/ROE/ROA 等の追加表示（今回は対象外） |

### ドキュメント・コードの乖離

- 乖離なし。テンプレートのみの変更で、CLAUDE.md 記載のアーキテクチャ（web/presenter → view/model）とは無関係。

---

## 2. プロトタイピング（ステップ 2）

### 採用手段：ask-question スキルによる認識合わせ（本 md 内記載）

ユーザーとの対話形式の認識合わせで構成案を1問ずつ確定・合意済み（2026-07-03）。

### 新バッジ構成

```
常時表示（デスクトップ: 列 / モバイル: グリッド）
  証券コード・会社名・提出日（変更なし）
  ＋ バッジ5種（すべてバッジ風スタイルで表示）
    - 割安比率（discountRateToDisplay）
    - グレアム指数（grahamIndex）
    - 最新企業価値（latestCorporateValue）        ← 新規（アコーディオンから移動）
    - 提出日株価平均（averageStockPrice）          ← 新規追加（ニュートラル表示、Good/Bad判定なし）
    - みんかぶ予想株価（forecastStock）            ← 新規追加（Good/Bad判定: forecastStock > averageStockPrice。Gate 2 後の追加要望で確定、2026-07-03）

アコーディオン展開時（トグルは維持）
  summaryChart（企業価値×株価推移グラフ、htmx 遅延ロード）のみ
  ※ バッジ3枚・詳細リンクは削除（会社名リンクが既に詳細への導線のため冗長）
```

---

## 3. 影響設計（ステップ 3）

### 3.1 変更属性チェック

| 変更属性 | 該当 | 理由 |
|---|---|---|
| シンボル参照（型・関数・定数） | **なし** | テンプレートのみの変更。`CorporateViewModel` の全フィールド（`averageStockPrice`・`forecastStock` 含む）は既存のまま使用 |
| 状態遷移・ライフサイクル | **あり（軽微）** | 各行の Alpine.js `x-data="{open:true}"` トグル自体は維持。展開時の中身がバッジ3枚からグラフのみに変わる。localStorage 永続化はこの一覧には元々なし（`open:true` は行ごとの初期値固定で、ページ再読み込みで常にリセットされる）ため移行考慮は不要 |
| データの形（スキーマ・既存値） | **なし** | DB 変更なし。`IndexPresenter` のモデル属性も変更なし |

### 3.2 参照層分析

変更対象は `fragments/index-table.html` 単一ファイル。
- `IndexPresenter.java` のモデル属性（`table` / `target` / `keyword` / `sortParam`）は変更なし
- `CorporateViewModel.java` のフィールドは追加・削除なし（既存の `averageStockPrice` / `forecastStock` / `latestCorporateValue` をテンプレート側で参照するだけ）
- `favorite-button` / `star-button` / `sortable-th` / `pagination` フラグメントは変更なし
- `data-chart-trigger`（summaryChart の htmx 遅延ロード）の id・エンドポイントは変更なし

### 3.3 状態層分析

各行の展開 state（`x-data="{open:true}"`）はページロードごとに初期化される揮発性の状態で、localStorage 永続化なし。中身の表示内容が変わるのみで、トグルの状態遷移ロジック自体（開↔閉）は変更しない。

| 遷移元 ＼ 操作 | `open=!open` |
|---|---|
| 展開中 | 閉じる（グラフを隠す） |
| 閉じ中 | 展開する（グラフを表示、`hx-trigger="intersect once"` で初回のみ summaryChart を取得） |

→ 既存の htmx 遅延ロードロジックと同一のため、新規ロジック考慮不要。

### 3.4 データ層分析

該当なし。DB・Flyway マイグレーション・YML 変更なし。

### 3.5 インフラ影響チェック

| カテゴリ | 結果 |
|---|---|
| 処理時間・タイムアウト | 変更なし（HTML/CSS のみ、DB クエリは既存の `viewService.findCompanyTable` のまま） |
| 外部サービス連携 | 変更なし |
| スキーマ変更 | なし |
| バッチ・非同期 | 変更なし |
| 依存ライブラリ追加 | なし |
| セキュリティ | なし（表示項目はすべて既存の `th:text` エスケープ経由。新規追加の `averageStockPrice`/`forecastStock` も `BigDecimal` のため XSS リスクなし） |

### 3.6 品質設計の三本柱

#### テスト戦略

- **単体・統合テスト**: Java 側変更なし → 不要
- **E2E（playwright-cli / e2e-check スキル）**: デスクトップ（1280px）+ モバイル（375px）でバッジ5種の常時表示、アコーディオン展開時のグラフのみ表示を画面確認
- **既存テスト**: 変更対象なし（テンプレートのみ）

#### セキュリティ方針

変更なし。入力・出力は既存の Thymeleaf エスケープのまま。

#### ドキュメント計画

- 本タスク md を完了後に更新
- `docs/notes/` 内の既存 md に影響なし

---

## Gate 1: 影響設計の承認

### レビュアー向けサマリ

- **判断してほしいこと**: `fragments/index-table.html` 単一ファイルの HTML 構造変更として影響範囲が正しく閉じているか。バッジ5種の常時表示化・アコーディオン簡素化の方針が妥当か。
- **重要な変更ポイント**:
    1. テンプレート1ファイルのみの変更（Java / DB / YML は一切変更しない）
    2. 常時表示バッジを2種→5種に拡張（割安比率・グレアム指数・最新企業価値・提出日株価平均・みんかぶ予想株価）
    3. アコーディオン展開時の中身をバッジ3枚+詳細リンクから、グラフのみに簡素化
    4. デスクトップは列を8列前後に拡張、モバイルはグリッドを5項目分に拡張（列数等の細部は実装時調整）
    5. ソート対象フィールドの拡張は行わない
- **確認してほしい観点**:
    1. 影響範囲が `fragments/index-table.html` のみに閉じているか
    2. 詳細リンク削除（会社名リンクへの集約）で利用者の導線に問題がないか

### 重点観点

- 参照層: なし（シンボル参照変更なし、既存 ViewModel フィールドのみ使用）
- 状態層: 展開トグルの中身変更のみ（ロジック自体は不変）
- データ層: なし
- インフラ: なし
- テスト戦略: E2E 画面確認のみ（Java テスト不要）
- セキュリティ: なし

### レビュアー記入欄

- 承認者: iori-oiso
- レビュー依頼日: 2026-07-03
- 回答日: 2026-07-03
- 結論: 合格
- コメント: 承認、実装に進める

---

## 4. テスト設計（ステップ 4）

### テストケース一覧（自然言語）

| # | テストケース | 種別 |
|---|---|---|
| T1 | デスクトップ（1280px）で会社一覧を開くと、各行にバッジ5種（割安比率・グレアム指数・最新企業価値・提出日株価平均・みんかぶ予想株価）が常時表示される | E2E スクリーンショット |
| T2 | デスクトップで行のトグルを開くと summaryChart のみが表示され、旧バッジ3枚・詳細リンクは表示されない | E2E スクリーンショット / DOM 確認 |
| T3 | モバイル（375px）でカード内にバッジ5種がグリッド表示される | E2E スクリーンショット |
| T4 | モバイルでカードを展開すると summaryChart のみが表示される | E2E スクリーンショット |
| T5 | 該当データが null の会社では各バッジが `-` 表示になる（NPE を起こさない） | E2E / 画面確認 |
| T6 | 既存のソート・ページング・検索・お気に入り／注目トグルの挙動が変わらない | E2E 回帰確認 |

### 状態遷移マトリクス（展開 state）

| 遷移元 ＼ 操作 | `open=!open` | ページ再読み込み |
|---|---|---|
| 展開中 | 閉じる | `open:true` に初期化（既存仕様どおり） |
| 閉じ中 | 展開する（初回 summaryChart 取得） | `open:true` に初期化（既存仕様どおり） |

→ 状態遷移ロジックは変更しないため、新規リスクなし。

### 既存テストとの関係

- Java ユニット/統合テスト: 変更なし（Java コード変更なし）
- 既存 E2E スクリプト: `e2e-tests/` 配下の一覧画面スクリプトは、セレクタが列追加の影響を受けないか確認要（DOM 構造変更のため）

---

## Gate 2: 完了条件の確認

### 運用ルート

**インライン**（テンプレート単一ファイル変更で完了条件が短く同期承認可能）

### 完了条件（機能・テスト・ドキュメントの 3 点セット）

**機能:**
- [x] デスクトップ・モバイルの常時表示にバッジ5種（割安比率・グレアム指数・最新企業価値・提出日株価平均・みんかぶ予想株価）が表示される
- [x] アコーディオン展開時はグラフ（summaryChart）のみが表示され、旧バッジ3枚・詳細リンクは削除されている
- [x] null データは `-` 表示でエラーにならない（既存の `?:` パターンを踏襲。テストデータでの実表示は全項目値ありのため目視は未実施、コードレビューで確認）
- [x] 既存のソート・ページング・検索・お気に入り／注目トグルが従来どおり動作する

**テスト:**
- [x] デスクトップ・モバイルで E2E 画面確認実施済み（`e2e-tests/reports/e2e-report-20260703-0715.md`）
- [x] 既存 Java テスト（`IndexPresenterTest`）が引き続き通過することを確認済み（20 tests, Failures: 0, Errors: 0）

**ドキュメント:**
- [x] 本タスク md の完了日・Gate 2 セクションを記入

**スコープ外宣言:**
- Java（`IndexPresenter` / `CorporateViewModel`) / DB / YML は変更しない → 変更なしで完了
- ソート対象フィールドの拡張は行わない → 対応なし
- PER/PBR/ROE/ROA 等の追加表示は対象外 → 対応なし

### 追加で判明した事項

- 実装直後、PC表示でバッジラベル（提出日株価平均・みんかぶ予想株価）が折り返され見た目が崩れる問題を検出し、`whitespace-nowrap` を追加して修正済み。
- summaryChart の `intersect once` トリガーが初回フルページロード時に発火しない事象を検出したが、変更前コードでも同一事象を確認（`htmx-ext-intersect` 未読み込みによる既存の潜在事象）。本タスクのリグレッションではなくスコープ外。別タスクでの対応を推奨。

### Gate 2 後の追加要望（2026-07-03）

初回 Gate 2 確認時に「みんかぶ予想株価もグレアム指数などと同様に Good/Bad で色分けしてほしい」との追加要望があり、以下で確定・実装・再検証済み。

- 判定基準: `forecastStock > averageStockPrice`（みんかぶ予想株価 > 提出日株価平均）で Good（青・サムズアップ）、そうでなければ Bad（赤・サムズダウン）
- 「提出日株価平均」は引き続きニュートラル表示（Good/Bad判定なし）で変更なし
- PC・モバイル両方に適用し、CSS 再ビルド・devサーバー再検証済み（`step-9-desktop-forecast-goodbad.png` / `step-10-mobile-forecast-goodbad.png`）

### レビュアー記入欄

- 承認者: iori-oiso
- 承認日: 2026-07-03
- 結論: 合格（みんかぶ予想株価のGood/Bad色分け追加後、再確認のうえ承認。コミットのみ実施、push/PR作成はなし）
