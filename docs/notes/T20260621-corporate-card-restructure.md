# Task T20260621: 銘柄詳細画面カード構成の再設計

- 着手日: 2026-06-21
- 完了日: -
- 担当: iori-oiso（計画・Gate 承認）+ Claude Code（実装・検証）
- 関連リンク:
    - スマホ UI 刷新マスター: [T20260502-mobile-ui-renewal.md](T20260502-mobile-ui-renewal.md)

---

## 1. 把握・整理（ステップ 1）

### 解決すべき課題（1 行）

**銘柄詳細画面の「表示情報」「期間別平均」「その他情報（6 タブ）」という曖昧・分断された構成を、情報の性質ごとに 5 つの明確なカードへ再編し、同種情報の散在と重複を解消する。**

### スコープ確定

| 区分 | 内容 |
|---|---|
| **コア（必ず実装）** | `corporate-v2.html` の ③表示情報カード・④期間別平均カード・⑤その他情報カードを削除し、以下 5 カードに再編:<br>①企業価値分析（統計数値 + 期間別平均 + 分析グラフ・テーブル）<br>②株価（株価数値 + 株価チャート + 株価予想グラフ）<br>③財務・指標（財務指標数値 + グレアム推移グラフ）<br>④財務諸表（BS/PL/CF テーブル）<br>⑤評価履歴（バリュエーション評価テーブル）<br>各カードは折りたたみ対応・localStorage 永続化（viewport 別デフォルト） |
| **後回し** | 各カード内のレイアウト詳細最適化（タブ→グリッド変換等）は別タスク |
| **対象外** | Java（Presenter / Controller）・DB・YML の変更。旧 localStorage キーのマイグレーション（ブラウザ側で自然に上書きされるため不要） |

### ドキュメント・コードの乖離

- 乖離なし。テンプレートのみの変更で、CLAUDE.md 記載のアーキテクチャとは無関係。

---

## 2. プロトタイピング（ステップ 2）

### 採用手段：認識合わせ済みの構成表（本 md 内記載）

ユーザーとの認識合わせで構成案を提示・承認済み（2026-06-21）。

### 新カード構成

```
バリュエーションサマリー（固定）     ← 変更なし
会社情報（折りたたみ）              ← 変更なし
─────────────────────────────────
[新] 企業価値分析（折りたたみ）
  統計数値（提出日・最新/平均企業価値・標準偏差・変動係数・対象年数）
  期間別平均（3年/5年/10年/全 × 平均企業価値/標準偏差/変動係数/割安度）
  企業価値推移グラフ（既存 analysisChartAll）
  分析結果テーブル（既存 analysisResults）

[新] 株価（折りたたみ）
  株価数値カード（株価取得日・最新株価・提出日株価平均・株価予想）
  株価チャート（30/90/180/365/全 サブタブ）+ OHLC テーブル
  株価予想グラフ（みんかぶ / 3 サブタブ）

[新] 財務・指標（折りたたみ、デフォルト閉）
  財務指標数値（割安比率・BPS・EPS・ROE・ROA・PER・PBR・株価企業価値率・グレアム指数）
  グレアム指数グラフ（180/365/全 サブタブ）

[新] 財務諸表（折りたたみ、デフォルト閉）
  BS / PL / CF テーブル（既存 financialStatements）

[新] 評価履歴（折りたたみ、デフォルト閉）
  バリュエーション評価テーブル（既存 valuations）
```

---

## 3. 影響設計（ステップ 3）

### 3.1 変更属性チェック

| 変更属性 | 該当 | 理由 |
|---|---|---|
| シンボル参照（型・関数・定数） | **なし** | テンプレートのみの変更。Thymeleaf 式・th:replace フラグメントの参照先は変わらない |
| 状態遷移・ライフサイクル | **あり（軽微）** | Alpine.js の localStorage キーが変更される（旧: `corporate.otherInfo.*`、新: 5 カード分の新キー）。ブラウザ側で旧キーは無視されるだけで破損しない |
| データの形（スキーマ・既存値） | **なし** | DB 変更なし。Presenter のモデル属性も変更なし |

### 3.2 参照層分析

変更なし。変更対象は `corporate-v2.html` 単一ファイル。
- Thymeleaf `th:replace` で参照している `fragments/tooltip` は変更なし
- `layout-v2.html` の `layout:fragment` は変更なし
- `CorporatePresenter.java` のモデル属性はすべてそのまま使用（追加・削除なし）
- `app.js` の `buildLine()` / `Chart()` 呼び出しは canvas id に依存するため、id を維持する限り変更不要

### 3.3 状態層分析

Alpine.js の折りたたみ state は localStorage に保存される。

| 旧キー | 新キー | 影響 |
|---|---|---|
| `corporate.displayInfo.sm/lg` | 廃止 | 旧値は残るが参照されなくなるだけ。破損なし |
| `corporate.periodAvg.sm/lg` | 廃止 | 同上 |
| `corporate.otherInfo.sm/lg` | 廃止 | 同上 |
| （新規）`corporate.cvAnalysis.sm/lg` | 新設 | 初回は viewport 別デフォルト値 |
| （新規）`corporate.stockCard.sm/lg` | 新設 | 同上 |
| （新規）`corporate.finIndicator.sm/lg` | 新設 | 同上 |
| （新規）`corporate.finStatement.sm/lg` | 新設 | 同上 |
| （新規）`corporate.evalHistory.sm/lg` | 新設 | 同上 |

旧キーはブラウザに残り続けるが実害なし。明示的なマイグレーションは不要。

### 3.4 データ層分析

該当なし。DB・Flyway マイグレーション・YML 変更なし。

### 3.5 インフラ影響チェック

| カテゴリ | 結果 |
|---|---|
| 処理時間・タイムアウト | 変更なし（JS/HTML のみ） |
| 外部サービス連携 | 変更なし |
| スキーマ変更 | なし |
| バッチ・非同期 | 変更なし |
| 依存ライブラリ追加 | なし |
| セキュリティ | なし（HTML 構造変更のみ。XSS 等リスク増なし） |

### 3.6 品質設計の三本柱

#### テスト戦略

- **単体・統合テスト**: Java 側変更なし → 不要
- **E2E（playwright-cli）**: デスクトップ（1280px）+ スマホ（375px）で各カード表示・折りたたみ動作をスクリーンショット確認
- **既存テスト**: 変更対象なし（テンプレートのみ）

#### セキュリティ方針

変更なし。入力・出力は既存の Thymeleaf エスケープのまま。

#### ドキュメント計画

- 本タスク md（この файл）を完了後に更新
- `docs/notes/` 内の既存 md に影響なし

---

## Gate 1: 影響設計の承認

### レビュアー向けサマリ

- **判断してほしいこと**: `corporate-v2.html` 単一ファイルの HTML 構造変更として影響範囲が正しく閉じているか。localStorage キー変更の扱いが妥当か。
- **重要な変更ポイント**:
    1. テンプレート 1 ファイルのみの変更（Java / DB / YML は一切変更しない）
    2. 旧カード 3 つ（表示情報・期間別平均・その他情報）を削除し、新カード 5 つに再編
    3. Alpine.js の localStorage キー 3 件を廃止・5 件を新設（旧値は無視されるだけ）
    4. canvas id（`analysisChartAll` 等）は維持し、`app.js` の変更は不要
    5. 優先度の低い 2 タブ（投資指標・財務諸表）は独立カードに昇格、デフォルト閉じ
- **確認してほしい観点**:
    1. 影響範囲が `corporate-v2.html` のみに閉じているか
    2. 旧 localStorage キーの廃止で利用者に不都合が生じないか

### 重点観点

- 参照層: なし（シンボル参照変更なし）
- 状態層: localStorage キー変更（旧 3 件廃止・新 5 件追加）— 破損リスクなし
- データ層: なし
- インフラ: なし
- テスト戦略: E2E スクリーンショットのみ（Java テスト不要）
- セキュリティ: なし
- ドキュメント: 本タスク md のみ更新

### レビュアー記入欄

- 承認者: iori-oiso
- レビュー依頼日: 2026-06-21
- 回答日: 2026-06-21
- 結論: 合格
- コメント: （口頭承認 2026-06-21）

---

## 4. テスト設計（ステップ 4）

### テストケース一覧（自然言語）

| # | テストケース | 種別 |
|---|---|---|
| T1 | デスクトップ（1280px）で銘柄詳細を開くと、5 つの新カードがすべて表示される | E2E スクリーンショット |
| T2 | 企業価値分析・株価カードはデスクトップでデフォルト展開されている | E2E スクリーンショット |
| T3 | 財務・指標・財務諸表・評価履歴カードはデスクトップでデフォルト閉じている | E2E スクリーンショット |
| T4 | スマホ（375px）で企業価値分析カードはデフォルト閉じ、タップで展開できる | E2E スクリーンショット |
| T5 | 旧カード（表示情報・期間別平均・その他情報）が画面に存在しない | DOM スナップショット確認 |
| T6 | 企業価値分析カード内に分析グラフ（canvas#analysisChartAll）が存在する | DOM スナップショット確認 |
| T7 | 株価カード内に株価チャート（canvas#stockChart30 等）が存在する | DOM スナップショット確認 |

### 状態遷移マトリクス（折りたたみ state）

| 遷移元 ＼ 操作 | toggle() | ページ再読み込み（localStorage あり） | ページ再読み込み（localStorage なし） |
|---|---|---|---|
| 展開中 | 閉じる & localStorage 保存 | 前回値を復元 | viewport 別デフォルト |
| 閉じ中 | 展開する & localStorage 保存 | 前回値を復元 | viewport 別デフォルト |

→ 既存の折りたたみパターンと同一のロジックを踏襲するため、新規ロジック考慮不要。

### 既存テストとの関係

- Java ユニット/統合テスト: 変更なし（Java コード変更なし）
- 既存 E2E スクリプト: `e2e-tests/` 配下のスクリプトはカード名変更の影響を受けない（CSS セレクタではなく URL ベース）

---

## Gate 2: 完了条件の確認

### 運用ルート

**インライン**（テンプレート単一ファイル変更で完了条件が短く同期承認可能）

### 完了条件（機能・テスト・ドキュメントの 3 点セット）

**機能:**
- [ ] 旧カード 3 つ（表示情報・期間別平均・その他情報）が削除されている
- [ ] 新カード 5 つ（企業価値分析・株価・財務・指標・財務諸表・評価履歴）が実装されている
- [ ] 各カードが折りたたみ対応・localStorage 永続化している
- [ ] デスクトップ: 企業価値分析・株価がデフォルト展開、残り 3 つがデフォルト閉じ
- [ ] スマホ: 全カードがデフォルト閉じ

**テスト:**
- [ ] デスクトップ・スマホでスクリーンショット E2E 実施済み
- [ ] 旧カードの DOM 非存在を確認済み
- [ ] 新カード内の canvas id が正しく存在することを確認済み

**ドキュメント:**
- [ ] 本タスク md の完了日・Gate 3 セクションを記入

**スコープ外宣言:**
- Java / DB / YML は変更しない
- 各カード内のレイアウト詳細最適化は別タスク

### レビュアー記入欄

- 承認者: iori-oiso（インライン承認）
- 承認日: 2026-06-21
- 結論: 合格
