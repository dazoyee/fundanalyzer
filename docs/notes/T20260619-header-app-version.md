# Task T20260619: ヘッダーにアプリバージョンを表示

- 着手日: 2026-06-19
- 担当: iori-oiso

---

## ステップ 1: 把握・整理

### 解決する課題

ヘッダーにアプリバージョンを表示し、運用・開発時にデプロイ済みバージョンを一目で確認できるようにする。

### 既存資産

| 資産 | 場所 | 状態 |
|---|---|---|
| バージョン配布 | `PresenterAdvice.java` | `applicationVersion` として全テンプレートに配布済み（変更不要） |
| バージョン設定 | `application.yml` の `app.version: @pom.version@` | Maven ビルド時に POM バージョンが注入済み（変更不要） |
| ヘッダーテンプレート | `templates/layout-v2.html` | 変更対象 |
| 既存テスト | `PresenterAdviceTest.java` | 既存テスト存在（変更不要） |

### スコープ確定

| 区分 | 内容 |
|---|---|
| **コア** | `layout-v2.html` のヘッダー右側（タイトル横・ダークモードボタン左）に `v{バージョン}` を表示 |
| **後回し** | なし |
| **対象外** | `PresenterAdvice.java` / `application.yml` の変更、SNAPSHOT 専用スタイル |

---

## ステップ 2: プロトタイピング

認識合わせ（2026-06-19 実施）で以下を確認済み:

- 表示位置: タイトル横・右寄せ（ダークモードボタンの左）
- 表示形式: `v` プレフィックス付き（例: `v2.3.2-SNAPSHOT`）
- モバイル: 全サイズで表示
- SNAPSHOT スタイル: 区別なし・常に同じスタイル

---

## Gate 1: 影響設計の承認

### レビュアー向けサマリ

- **判断してほしいこと**: 変更は `layout-v2.html` の1箇所のみ。影響範囲分析・インフラ影響・品質設計の三本柱に問題がないか確認してほしい。
- **重要な変更ポイント**:
  - `layout-v2.html` のヘッダー `<header>` 内に `th:text="'v' + ${applicationVersion}"` を追加
  - `applicationVersion` は `PresenterAdvice` が既にすべてのテンプレートに配布済みのため Java 側の変更なし
  - Thymeleaf の `th:text` により XSS は自動エスケープ済み
- **確認してほしい観点**:
  - テンプレート変更のみで他への副次影響がないか
  - baseline スナップショットの更新が必要かどうか

### 影響範囲分析（変更属性チェック）

| 変更属性 | 判定 | 理由 |
|---|---|---|
| シンボル参照の変更/追加 | **該当なし** | 既存の `applicationVersion` モデル属性を参照するだけで、シンボル定義・型・関数シグネチャの変更なし |
| 状態遷移・ライフサイクルの変更/追加 | **該当なし** | 状態遷移を扱う変更ではない |
| データの形（スキーマ・既存値）の変更 | **該当なし** | DB・スキーマへの変更なし |

3 属性すべて該当なし。形式的な影響チェックのみ。

### インフラ影響チェック

すべての項目で「該当なし」を確認。

- A. 処理時間: 既存テンプレートに文字列追加のみ、負荷変動なし
- B. 外部サービス連携: 新規呼び出しなし
- C. データストア・スキーマ: 変更なし
- D. バッチ・非同期処理: 変更なし
- E. リソース: 影響なし
- F. 可用性: 影響なし
- G. セキュリティ: `th:text` で自動エスケープ済み、機密情報なし
- H. 監視: 変更なし
- I. デプロイ: 設定変更なし（`app.version` は既存設定）
- J. 互換性・依存関係: 新規ライブラリ追加なし

### 品質設計の三本柱

| 柱 | 採用方針 |
|---|---|
| **テスト戦略** | テンプレート変更のみのため単体テスト不要。Playwright スナップショット baseline を更新して視覚的回帰を防ぐ |
| **セキュリティ方針** | `th:text` による自動エスケープのみで十分。追加対応不要 |
| **ドキュメント計画** | 本 md（T20260619）のみ更新。API 仕様・ER 図・状態遷移図への影響なし |

### レビュアー記入欄

- 承認者: iori-oiso
- レビュー依頼日: 2026-06-19
- 回答日: 2026-06-19
- 結論: 合格
- コメント:

---

## Gate 2: 完了条件の確認

### 運用ルート

**省略**（小タスク基準達成）

小タスク基準:
- [x] 影響範囲が単一ファイル・単一関数（`layout-v2.html` の `<header>` 内1行追加）
- [x] テストケース数が 3 件以下で済む（Playwright baseline 更新1件のみ）
- [x] ドキュメント更新が不要（本 md のみ）
- [x] 既存仕様への影響なし
- [x] セキュリティ・性能への影響なし

### 完了条件（機能 + テスト + ドキュメントの3点セット）

**機能要件**:
- [ ] `layout-v2.html` のヘッダーにバージョン文字列が `v{version}` 形式で表示される
- [ ] タイトル右側（ダークモードボタンの左）に配置されている
- [ ] デスクトップ・モバイル両方で表示される

**テスト要件**:
- [ ] Playwright スナップショット baseline を更新（`ManualMobileScreenshotTest` で撮影し `src/test/resources/playwright-baselines/` を上書き）

**ドキュメント要件**:
- [ ] 本 md（Gate 3 セクション）を更新

**スコープ外**:
- `PresenterAdvice.java` の変更
- SNAPSHOT 専用スタイル追加
- 新規テストクラスの作成（テンプレート変更のみのため不要）

---

## Gate 3: 最終確認

### レビュアー向けサマリ

- **判断してほしいこと**: 利用者視点でバージョン表示が自然か・他機能への副次影響がないかを確認してほしい。
- **重要な変更ポイント**:
  - `layout-v2.html` の `<header>` 内に1行追加のみ
  - 全ページ共通レイアウトへの変更のため、全画面で反映される
- **確認してほしい観点**:
  - 画面表示が認識合わせ時のプロトタイプと一致しているか
  - 既存画面（index / corporate / valuation / edinet-list 等）に副次影響がないか

### 重点観点

- 差分レビュー: `layout-v2.html` の `<header>` 内に3行追加のみ（`th:text` + `aria-hidden`）
- 多軸検証結果:
  - コード品質: `th:text` XSS対策OK・null安全OK・`aria-hidden="true"` を追加適用
  - テストの構造品質: 既存747件全合格・新規テスト不要（テンプレートのみ）
  - 機能完全性: 完了条件（位置・形式・全サイズ表示）すべて満たす
  - セキュリティ: 自動エスケープ済み・機密情報なし
  - ドキュメント整合性: タスクmd更新済み
- 動作確認結果: （Gate 3 時に記載）
- 副次影響: （Gate 3 時に記載）

### レビュアー記入欄

- 承認者: iori-oiso
- レビュー依頼日: 2026-06-19
- 回答日: 2026-06-19
- 結論: 合格
- コメント: ヘッダーからフッターに変更。既存フッターテキストをバージョンに置き換え。

---

## 更新履歴

- 2026-06-19: タスク着手、Gate 1 分析完了
- 2026-06-19: 実装完了（フッターにバージョン表示、既存フッターテキスト置き換え）、Gate 1 / Gate 2（省略）/ Gate 3 通過
